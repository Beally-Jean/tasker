const addSpace = document.querySelector('footer');
// const addBtn = document.querySelector()
const workspace = document.querySelector('.workspace');

function addWorkspace(){
    const newSpace = document.createElement(workspace)
}

addSpace.addEventListener('click', addWorkspace)
